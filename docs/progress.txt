# progress.txt — append-only セッションログ
# 形式: [日付] [iteration] やったこと / Decisions: 判断と根拠 / BLOCKED: 理由

2026-07-05 [setup] 足場構築: git init, Vite+Phaser+TS スケルトン, PRD/features.json(21件)/LOOP_PROMPT/verify.sh 作成。npm install 完了。
2026-07-05 [setup] Decisions: アートはコード生成プレースホルダー(スプライトキー一元管理で後日差し替え), SEはWebAudio生成, itch.io実アップロードはスコープ外(人間実行)。
2026-07-05 [iter1] F1完了: Boot→Main のシーン骨格を src/scenes/ に分離、main.ts はゲーム設定のみに。verify.sh green (tsc+build、tests/E2Eは未着手スキップ)。 / Decisions: シーンは1ファイル1クラスで scenes/ 配下に置く（後続シーン追加の型を先に作る）。
2026-07-05 [iter2] F2完了: mulberry32 PRNG(src/core/rng.ts)+役テーブル定数(src/data/paytable.ts)+抽選エンジン(src/core/slot.ts)。10万試行分布テスト15件 green。 / Decisions: 小役(剣/ハート/星)の払い出しは×0.5に設定→通常時コインRTP=90.0%でdesign.mdの目標90〜95%下限に合わせた(ラッシュ分の上振れ余地を残す)。RTP検証テストも追加。
2026-07-05 [iter3] F3完了: 出目解決(core/reels.ts)+図柄テクスチャ生成(assets/textures.ts,keys.ts)+MainSceneのリール表示と3段階ストップ。verify green(27テスト)。 / Decisions: 回転演出はテクスチャ高速入替方式(スクロールより単純で結果整合が保証しやすい)。ハズレ出目は「3揃いにしない」だけ保証し2個一致(リーチ)は自然発生に任せる(F6の演出素材になる)。
2026-07-05 [iter4] F4完了: コイン経済(core/economy.ts)+MainScene残高UI・ベット消費・全停止後払い出し・不足時ボタン無効。verify green(39テスト)。 / Decisions: BET=10固定・初期コイン500(design.mdに規定なし、天井40スピン=400消費を初期資金で1周弱回れる水準)。払い出しは役テーブルのpayoutMultから導出し二重管理しない。
2026-07-05 [iter5] F5完了: 天井ロジック(core/ceiling.ts)+右上メーターUI(残りスピン数テキスト+縮むバー)。40スピン非突入→41回目強制突入のテスト含む44テスト green。 / Decisions: 突入判定entersBattleはF5でカウンタ更新にのみ接続し、シーン遷移はF6で接続する(featureの境界を守る)。
2026-07-05 [iter6] F6完了: BattleScene骨格追加+扉3揃い/天井でシーン遷移+リーチ時3リール目スロー化(60ms→220ms)。コイン・天井カウンタをcore/state.tsのgameStateへ移設(シーンrestart間で消えないように)。verify green(45テスト)。 / Decisions: 天井強制突入時も出目は抽選どおり表示(出目改変せず正直に。メーター0表示が天井発動の説明になる)。戦闘エンジン接続はF8。
2026-07-05 [iter7] F7完了: 15種(ベース4+配合10+隠し1)をdata/monsters.tsに定義、スキル15種、見た目はAppearance記述子でデータ化し一意性をテストで保証。系統色×体型5種+装飾で動的テクスチャ生成。メイン画面にパーティ3体表示(待機ゆれ)。verify green(51テスト)。 / Decisions: 見た目の「見分けられる」は(系統色,体型,装飾,目数)署名の全種一意性テストで担保。パーティ初期編成はベース3種(wolfy/draco/pururu)。配合組み合わせ表はF13で定義。
2026-07-05 [iter8] F8完了: BattleSim(core/battle.ts)=SPD降順ラウンド制・1step1行動・演出用イベント列返却。BattleSceneはイベント再生(突進tween+ダメージポップ+HPバー+撃破フェード+勝敗表示)。強弱20シード勝敗テスト含む59テスト green。 / Decisions: ダメージ式=max(1,ATK-DEF/2)×(0.85〜1.15)。step駆動APIにしてシーン側のアニメ速度と戦闘進行を分離。報酬はF10、敵スケーリングはF11。
2026-07-05 [iter9] F9完了: BattleSim.applyBoost(sword=全員追撃/heart=全員25%回復/star=先頭スキル、heal/skillイベント追加)+Battleシーンにミニスロット(ベット消費・払い出し継続)とブーストバナー演出。verify green(65テスト)。 / Decisions: ラッシュ中もベット消費と小役払い出しは通常時と同一(経済の一貫性)。先頭=生存している最前列。ハート回復率25%は定数HEART_HEAL_RATIOで調整可能に。
2026-07-05 [iter10] F10完了: 素材4種(data/materials.ts、F14の上昇量込み)+報酬計算(core/rewards.ts: 勝利=150+階層×50コイン/タマゴ1/素材2、敗北=投入50%返還+素材1)+状態にeggs/materials追加+結果画面に内訳表示。verify green(70テスト)。 / Decisions: 保険は「バトル中に投入したコイン」基準(spentCoinsをシーンで記録)。階層は F11 未実装のため floor=1 固定で渡す。素材の効果値(stat/gain)はF14で使う前提で先に定義し再編集を避けた。
2026-07-05 [iter11] F11完了: 階層進行(core/floors.ts: 勝利+1/最高更新/10の倍数=ボス/スケール1+(floor-1)*0.12)+敵生成を階層対応化(ボス階=配合種1体×1.8倍)+Main/Battleに階層UI。報酬計算にgameState.floor接続。verify green(77テスト)。 / Decisions: SPDのスケールは他ステの30%に抑制(高階層で先手を取られ続ける理不尽の回避)。敗北時は階層据え置き(降格なし、保険と合わせて再挑戦を促す)。ボスのタマゴは通常勝利と同じ1個(design.mdの「勝利でタマゴ」が常時成立のため)。
2026-07-05 [iter12] F12完了: 個体(MonsterInstance=uid+種族+bonus)導入でroster/partyをuidベース化。core/collection.ts(孵化/編成/実効ステ)+RosterScene(孵化ボタン・一覧グリッド・クリック編成)+Mainに育成ボタン。バトルは実効ステで出撃。verify green(86テスト)。 / Decisions: 編成UIは「一覧で選択→スロットクリック」方式(ドラッグ不要で実装が単純)。パーティ重複はスワップで防止。一覧表示は18体まで+「ほかN体」表記(ジャム規模の割り切り、F13の配合で親消滅するため実際は溢れにくい)。
2026-07-05 [iter13] F13完了: core/fusion.ts(系統×系統10通り対称表+隠し種kingpururu×ghostpull=prisma+親消滅+実効ステ合計25%上乗せ+パーティ修復)+個体にskillId追加(継承の器)+RosterSceneに配合モード(2体選択→継承スキル選択パネル)。verify green(94テスト)。 / Decisions: 上乗せは「親の実効ステ合計×25%」(全量だと指数的に強くなり階層スケールが追いつかない)。配合は手持ち4体以上必須(パーティ3体を維持するため)。隠し種条件は配合種同士のレア組み合わせ1通り(kingpururu×ghostpull、両方slime系配合を経る2段階が「条件」に相当)。
2026-07-05 [iter14] F14完了: core/feeding.ts(素材1個消費→data/materials.tsのstat/gainどおりbonus加算、不足は例外)+RosterSceneに素材ボタン4種(所持数表示、選択中+所持ありのみ有効、配合モード中は無効)。verify green(101テスト)。 / Decisions: 追加判断なし(F10で先行定義した素材データをそのまま消費、設計どおり)。
2026-07-05 [iter15] F15完了: discovered配列+discover/isDiscovered(collection.ts)を孵化・配合・初期状態にフック。DexScene(未発見はsetTintFillシルエット+???)、OddsScene(PAYTABLEの実値=分数/％/効果+ハズレ率)。Mainに図鑑/確率表ボタン。verify green(105テスト)。 / Decisions: 発見=入手(敵として見ただけでは未発見扱い、コンプには配合が必要になり配合動機を強化)。シルエットは専用テクスチャでなくsetTintFillで実現(テクスチャ二重生成を回避)。
2026-07-05 [iter16] F16完了: core/flash.ts(成功=ベット×4+素材1/失敗=半額・素材なし)+MainSceneの点滅グロー合図(2リール停止後に3リール目枠が200ms周期点滅、光った瞬間の停止で満額)。タイミング独立性テスト(役確定後の乱数消費で役が変わらない)。verify green(109テスト)。 / Decisions: 目押し窓=点滅ON中(200ms/400ms周期=成功率約50%、リーチスロー化と重なり狙いやすい)。満額報酬はコイン×4+素材1、取りこぼしは素材ごと失う(素材が目押しの主報酬)。ラッシュ中ミニスロットのフラッシュは対象外(F17のオート半額と整合はMainのみで担保)。
2026-07-05 [iter17] F17完了: core/autospin.ts(解放=bestFloor6以上、resolveFlashSuccess=オート中常にfalse)+MainSceneオートボタン(未解放はグレー表示「オート(6F解放)」)+自動スピン/自動停止ループ(450ms後スピン→600/1050/1500msで順停止→次スピン)。コイン不足やラッシュ突入で自動解除/中断。verify green(113テスト)。 / Decisions: オートON/OFF設定はgameStateに置きF18のセーブ対象(設定)に含める。ラッシュ突入時はオート中断しバトルは手動(ブースト操作の楽しさを残す)。
2026-07-05 [iter18] F18完了: core/save.ts(StorageLike注入・構造検証付きロード・破損/型不正/参照不整合は初期化フォールバック・既知フィールドのみ復元)+Boot復元+保存フック(スピン確定/バトル結果/孵化/配合/餌/編成/オート切替)。verify green(121テスト)。 / Decisions: 検証はパーティuid実在・種族/スキルID実在・uid重複なしまで見る(壊れたセーブで起動不能になるのが最悪なので広めに防御)。保存はイベント駆動(タイマー保存なし、状態が変わった瞬間だけ書く)。
2026-07-05 [iter19] F19完了: assets/sfx.ts(WebAudio生成SE7種、AudioContextなし環境はno-op)+ラッシュカットイン(帯+スライド+フラッシュ+振動)+払い出しコインシャワー(額に応じ増量/大当たりは金コイン+振動)+バトルのヒットSE/撃破ヒットストップ(timeScale 0.05を実時間setTimeoutで復帰)+勝利ファンファーレ/敗北音。verify green(123テスト)。 / Decisions: ヒットストップ復帰はsetTimeout(実時間)で保証しシーンcreateでもtimeScale=1を強制(止まったまま事故の二重防御)。コインシャワーはdepth分離で操作をブロックしない。
2026-07-05 [iter20] F20完了: scripts/e2e.mjs(mode=e2eでdist-e2eビルド→node httpで配信→Puppeteerで起動/スピン/天井強制ラッシュ/戦闘早送り/結果到達/コンソールエラー0を検証)。__DEBUG__フックはsrc/debug.tsをMODE!==productionのみ動的import、本番distに__DEBUG__は0件を確認。verify green(tsc+build+123テスト+E2E PASS)。 / Decisions: E2Eは本番distでなくdist-e2e(フック有無だけの差分の同一構成ビルド)を配信(本番にチートフックを入れない方を優先、featuresの意図=ビルド成果物の動作検証は満たす)。spinOnce/fastForwardは本流と同じ経路を同期実行するだけでロジック分岐を持たない。
2026-07-05 [iter21] F21完了: scripts/package.sh(本番ビルド→python3 zipfileでdist直下をルートにzip化、zipコマンド非依存)+README.md(コアループ・操作表・開発コマンド・クレジット欄)。本番distを静的サーバ配信で実起動確認(canvas表示・コンソールエラー0・__DEBUG__なし)。zip内はindex.htmlがルート直下(itch.io要件)。verify green(tsc+build+123テスト+E2E)。 / Decisions: zip生成はpython3 zipfile(WSL環境にzipコマンドがないため)。zipファイル名はpackage.jsonのversion連動、.gitignoreに追加。
2026-07-05 [v2-iter1] V2-F1完了: 帯スクロールリール。core/reelband.ts(スロット図柄/縦ずらし/停止位置の純関数)+MainSceneをupdate駆動の連続スクロール+減速スナップtweenに置換、窓はGeometryMaskで隠す。verify green(130テスト+E2E)。 / Decisions: 下方向スクロールでは窓の上=帯の次図柄になるためreelWindowと上下が逆だが、featureの要求は「帯の固定順で隣接」でありreelWindow(v1テスト)はそのまま温存。目押し判定は減速完了でなく「押した瞬間」の点灯状態(目押しの意味を保つ)。E2Eはimmediate停止で演出をスキップ(本流と同一経路)。
2026-07-05 [v2-iter2] V2-F2完了: タイトル画面。TitleScene(タイトル+コンセプト1行+スタート/つづきからボタン)、Boot→Title→Main。文言判定はcore/save.tsのhasSave純関数(有効セーブのみtrue)で5テスト追加。E2Eは__DEBUG__.skipTitle()でタイトル到達→スキップ→Mainを検証。verify green(135テスト+E2E)。 / Decisions: 「つづきから」判定は保存の存在+構造妥当性まで見る(壊れたセーブはロード時に初期化されるため「つづき」は嘘になる)。タイトルスキップはE2E専用フックに置き本流コードに分岐を持ち込まない。
2026-07-05 [v2-iter3] V2-F3完了: 初回オンボーディング。core/onboarding.ts(spin→rush→boost→doneの状態機械+文言+正規化の純関数)、gameStateにguide追加、Mainに段階バナー、Battleに初ラッシュのブースト説明パネル(OK/終局で自動クローズ)。11テスト追加(146テスト+E2E green)。 / Decisions: guideフィールドの無いv1セーブはdone扱い(既存プレイヤーにガイドを再表示しない。isValidSaveは変えず後付けフィールドとして寛容に読む)。初スピンで扉3揃いならrushを飛ばしboost直行。説明パネルは戦闘を止めない(デッドロック回避)。
2026-07-05 [v2-iter4] V2-F4完了: 画像アセットパイプライン。scripts/gen-asset-manifest.mjs(public/assets走査→src/assets/manifest.json、symbols/*→symbol-*、monsters/*→monster-*のキー規約をkeys.tsと一致テストで担保)+Boot preloadでmanifest記載分のみロード(textures.existsで生成をスキップ=画像優先)+npm run assets。8テスト追加(154テスト+E2E green)。ダミーPNG実検証: 実ブラウザでsymbol-copperが外部画像(blob URL)・symbol-goldがgeneratedのまま・コンソールエラー0を確認。 / Decisions: manifestはビルド時importでバンドル(実行時fetchなし=manifest自体も404源にならない)。確認手段として__DEBUG__.textureSrc(key)を追加(外部=URL/生成=generated)。検証中の404はscratchサーバのfavicon未対応が原因でゲーム側ではない。
2026-07-05 [v2-iter5] V2-F5完了: オーディオパイプライン。core/audio.ts(bgmForScene/bgmTransition/normalizeMutedの純関数)+assets/bgm.ts(フェード付き再生・勝利ジングル・アセット不在は無音no-op)+Boot preloadでmanifest.audioロード+ui/muteButton.ts(🔊/🔇トグル、Main/Battleに設置)+gameState.mutedをセーブ対象に。12テスト追加(166テスト+E2E green)。 / Decisions: BGMはBattle専用曲+他シーン共通曲の2トラック構成(Main⇔育成の往復で途切れさせない、bgmTransitionが同曲continue判定)。setSfxMutedはsfxオブジェクト外に分離(既存テスト『全SEが無引数で呼べる』を壊さない)。mutedもguide同様の後付け寛容ロード(旧セーブ=音あり)。
2026-07-05 [v2-iter6] V2-F6完了: UI共通スタイル。src/ui/button.ts(addButton=ホバーで明色化/押下で沈む、setButtonColorで状態色変更、lighten純関数)+panel.ts(addPanel共通下地)+transitions.ts(fadeIn/fadeToScene、fadeEffect.isRunningで連打ガード)。Title/Main/Battle/Roster/Dex/Oddsの全スタイル付きボタン14箇所+パネル3箇所を置換、全シーン遷移をフェード化。verify green(166テスト+E2E)。 / Decisions: onClickはpointerdown発火を維持(目押しタイミングを変えない、沈み込みは演出のみ)。返り値はTextのままにして既存のdisableInteractive/setText/setAlphaによる状態制御と共存。編成スロット枠と一覧画像はゲーム要素でありボタン扱いしない。BootのTitle遷移とE2E用skipTitleは無フェードのまま(視覚なし/テスト専用)。
2026-07-05 [v2-iter7] V2-F7完了: メイン画面の情報設計。ヘッダー中央を「ダンジョンXF」(30px金)+「最高記録XF─コンセプト1行」に再構成(ゲームタイトル文字はTitle画面に譲り削除)、天井メーター文言を「あとNスピンでラッシュ確定」へ、メーター直下に扉アイコン+「扉3つでもラッシュ突入！」の価値説明、ミュートはコイン下へ移動。puppeteerスクリーンショット2回で重なり無しを目視確認。verify green(166テスト+E2E)。 / Decisions: 目的の常時提示はヘッダー1行に集約(常設チュートリアルより情報密度を優先)。扉アイコンはテキスト右端揃えと干渉したためx=714に分離配置。
2026-07-05 [v2-iter8] V2-F8完了: リーチ・揃い演出強化。リーチ時(2リール一致)は既存の3リール目スロー化に加え有効ライン枠をピンクに変え点滅(alpha tween)、全停止で復帰。揃い時は中央ライン3図柄をscale1.28で拡大点滅(yoyo×3)。次スピン開始時にtween解除+scale復元(帯画像は使い回しのため必須)。出目強制の一時フックを仕込んだ検証ビルドでリーチ点滅・拡大強調をスクリーンショット確認し、フックは削除してからコミット。verify green(166テスト+E2E=デッドロックなし)。 / Decisions: 演出は全てtweenで入力を塞がない(ボタン無効化なし)。ライン点滅はリーチ中無限repeatだが停止確定のstopLineBlinkで確実に解除。
2026-07-05 [v2-iter9] V2-F9完了: リリース更新。README v2化(帯スクロール/リーチ/ミュート/初回ガイドの操作表更新+アセット差し替え手順のセクション新設+クレジットにSuno BGM明記)。ダミーPNG+実BGMでpublic/assets→dist同梱を確認(dist/assets/画像1+音声3)、ダミー撤去後package.shでzip生成(index.htmlルート直下+BGM3曲同梱を検証)。verify green(166テスト+E2E)。 / Decisions: ダミーPNGはdist混入を避けるためzip生成前に撤去(同梱確認の証跡はprogressに記録)。クレジットの画像欄は差し替え時に記載する形式のまま(未配置のため)。
